server:
  port: 8082

spring:
  application:
    name: subtraction-service
  cache:
    type: simple

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.calculator: DEBUG